{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend exec -- vite --port 8080 --host",
    "build": "npm --prefix frontend exec -- vite build",
    "build:dev": "npm --prefix frontend exec -- vite build --mode development",
    "preview": "npm --prefix frontend exec -- vite preview"
  }
}
