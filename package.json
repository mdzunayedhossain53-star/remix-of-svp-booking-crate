{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npx vite --port 8080 --host",
    "build": "cd frontend && npx vite build",
    "build:dev": "cd frontend && npx vite build --mode development",
    "preview": "cd frontend && npx vite preview"
  }
}
