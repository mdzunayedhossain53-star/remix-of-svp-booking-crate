{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev -- --port 8080 --host",
    "build": "npm --prefix frontend run build",
    "build:dev": "npm --prefix frontend run build:dev",
    "preview": "npm --prefix frontend run preview"
  }
}
